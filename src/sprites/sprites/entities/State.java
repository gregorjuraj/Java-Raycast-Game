package sprites.sprites.entities;

/**
 * Enum State definuje možné stavy entity.
 * Používa sa na určenie správania a animácií entity.
 */
public enum State {
    STAND,
    PATROL,
    CHASE,
    DEAD,
    SHOOT;
}